%p#notice= notice

%p= "Name: #{@page.name}"
%p= "Slug: #{@page.slug}"

%ul
- for @page.reasons do |reason|
  %li= reason.text

= link_to 'Edit', edit_page_path(@page)
\|
= link_to 'Back', pages_path
